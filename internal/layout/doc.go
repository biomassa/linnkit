// Package layout models the LinnStrument grid (25 x 8 pads plus the control column)
// and generates and scores layout candidates for a scale: uniform row offset,
// generator-based (MOS) and no overlap.
package layout
